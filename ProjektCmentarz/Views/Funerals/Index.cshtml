@model IEnumerable<ProjektCmentarz.Models.Funeral>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FuneralDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FuneralPlot)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Deceased)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FuneralPriest)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FuneralDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FuneralPlot.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Deceased.FirstName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FuneralPriest.FirstName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
